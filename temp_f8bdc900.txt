/* global React, Hotspot, useTracker */
/*
  Interactive flow screens — purpose-built for the demo.
  Each screen accepts a `go` callback to navigate to the next step.
*/

const { useState, useEffect, useRef } = React;
const V1 = window.V;

// ============================================================
// 01 · ONBOARDING — A → B → C → Timetable
// ============================================================

const FlowOnboardA = ({ go, showHints }) => (
  <div className="phone">
    <div className="phone-inner">
      <div className="statusbar"><span>9:42</span><span>••• 􀟝􀛨</span></div>
      <div style={{ padding: "20px 18px", display: "flex", flexDirection: "column", gap: 14, height: "100%" }}>
        <div className="label">welcome screen · 1 of 3</div>
        <div style={{ fontSize: 30, lineHeight: 1.05 }}>
          Never wonder<br />if you'll make it<br />to class on time.
        </div>
        <div className="box scribble" style={{ height: 130, border: "1.8px dashed var(--pencil)", display: "grid", placeItems: "center" }}>
          <span style={{ fontSize: 11, color: "var(--pencil)" }}>
            [ illustration: tiny student crossing campus ]
          </span>
        </div>
        <div style={{ marginTop: "auto", display: "flex", flexDirection: "column", gap: 8 }}>
          <Hotspot label="onboard:connect-timetable" onTap={() => go("ob-b")} highlight={showHints} hint="tap here">
            <button className="btn primary" style={{ width: "100%", pointerEvents: "none" }}>{V1("getStarted", "Connect my timetable →")}</button>
          </Hotspot>
          <Hotspot label="onboard:skip" onTap={() => go("ob-c")}>
            <button className="btn ghost" style={{ width: "100%", pointerEvents: "none" }}>{V1("skipForNow", "Skip for now")}</button>
          </Hotspot>
        </div>
      </div>
    </div>
  </div>
);

const FlowOnboardB = ({ go, showHints }) => (
  <div className="phone">
    <div className="phone-inner">
      <div className="statusbar"><span>9:42</span><span>••• 􀟝􀛨</span></div>
      <div style={{ padding: "16px 14px", display: "flex", flexDirection: "column", gap: 12, height: "100%" }}>
        <div className="label">3-step intro · step 2/3</div>
        <div style={{ display: "flex", gap: 4 }}>
          <div style={{ flex: 1, height: 4, background: "var(--accent)", borderRadius: 2 }} />
          <div style={{ flex: 1, height: 4, background: "var(--accent)", borderRadius: 2 }} />
          <div style={{ flex: 1, height: 4, background: "var(--pencil-light)", borderRadius: 2 }} />
        </div>
        <div style={{ fontSize: 26, lineHeight: 1.1 }}>
          Tap any class.<br />Get a campus route.
        </div>
        <div className="box" style={{ padding: 0, overflow: "hidden", flex: 1 }}>
          <div style={{ padding: "8px 10px", borderBottom: "1.5px dashed var(--pencil-light)", fontSize: 12 }}>
            MON · 10:00 — STAT2003
          </div>
          <div style={{ padding: 10, display: "flex", gap: 8, alignItems: "center" }}>
            <div className="pill accent">→ 50-N202</div>
            <span style={{ fontSize: 11, color: "var(--pencil)" }}>tap to navigate</span>
          </div>
          <div className="map-bg" style={{ height: 160, margin: 8, borderRadius: 6, border: "1.5px dashed var(--pencil)" }} />
        </div>
        <Hotspot label="onboard-b:next" onTap={() => go("ob-c")} highlight={showHints} hint="continue">
          <button className="btn primary" style={{ width: "100%", pointerEvents: "none" }}>Next →</button>
        </Hotspot>
      </div>
    </div>
  </div>
);

const FlowOnboardC = ({ go, showHints }) => {
  const [loc, setLoc] = useState(false);
  const [notif, setNotif] = useState(false);
  const both = loc && notif;
  return (
    <div className="phone">
      <div className="phone-inner">
        <div className="statusbar"><span>9:42</span><span>••• 􀟝􀛨</span></div>
        <div style={{ padding: "18px 16px", display: "flex", flexDirection: "column", gap: 10, height: "100%" }}>
          <div className="label">permissions · step 3/3</div>
          <div style={{ display: "flex", gap: 4 }}>
            <div style={{ flex: 1, height: 4, background: "var(--accent)", borderRadius: 2 }} />
            <div style={{ flex: 1, height: 4, background: "var(--accent)", borderRadius: 2 }} />
            <div style={{ flex: 1, height: 4, background: "var(--accent)", borderRadius: 2 }} />
          </div>
          <div style={{ fontSize: 24, lineHeight: 1.1 }}>
            Two quick things<br />before we start.
          </div>
          <div className="box" style={{ display: "flex", gap: 10, alignItems: "flex-start" }}>
            <div style={{ width: 28, height: 28, borderRadius: 6, border: "1.5px solid var(--ink)", display: "grid", placeItems: "center", fontSize: 14 }}>📍</div>
            <div style={{ flex: 1 }}>
              <div style={{ fontWeight: 600, fontSize: 13 }}>Location, while using app</div>
              <div style={{ fontSize: 11, color: "var(--pencil)" }}>So we know if your pace is on track.</div>
            </div>
            <Hotspot label="onboard-c:allow-location" onTap={() => setLoc(true)}>
              <div className={"pill " + (loc ? "ok" : "accent")} style={{ fontSize: 11, pointerEvents: "none" }}>
                {loc ? "✓ Allowed" : "Allow"}
              </div>
            </Hotspot>
          </div>
          <div className="box" style={{ display: "flex", gap: 10, alignItems: "flex-start" }}>
            <div style={{ width: 28, height: 28, borderRadius: 6, border: "1.5px solid var(--ink)", display: "grid", placeItems: "center", fontSize: 14 }}>🔔</div>
            <div style={{ flex: 1 }}>
              <div style={{ fontWeight: 600, fontSize: 13 }}>Departure reminders</div>
              <div style={{ fontSize: 11, color: "var(--pencil)" }}>One per class. No spam, promise.</div>
            </div>
            <Hotspot label="onboard-c:allow-notif" onTap={() => setNotif(true)}>
              <div className={"pill " + (notif ? "ok" : "accent")} style={{ fontSize: 11, pointerEvents: "none" }}>
                {notif ? "✓ Allowed" : "Allow"}
              </div>
            </Hotspot>
          </div>
          <div className="box dashed soft" style={{ padding: 8, fontSize: 11, color: "var(--pencil)" }}>
            You can change these anytime in Settings.
          </div>
          <Hotspot label="onboard-c:done" onTap={() => go("timetable")} highlight={showHints && both} hint={both ? "you're set" : ""}
            style={{ marginTop: "auto", opacity: both ? 1 : 0.5, pointerEvents: both ? "auto" : "none" }}>
            <button className="btn primary" style={{ width: "100%", pointerEvents: "none" }}>
              {both ? "Got it, let's go" : "Allow both to continue"}
            </button>
          </Hotspot>
        </div>
      </div>
    </div>
  );
};

// ============================================================
// 02 · TIMETABLE — list (B-style) with class-detail modal (C-style)
// ============================================================

const CLASSES = [
  { code: "STAT2003", time: "11:00", duration: "1hr", room: "50-N202", building: "Hawken (50)", inMin: 18, walk: 6, isNext: true },
  { code: "CSSE2310", time: "13:00", duration: "2hr", room: "78-217", building: "GP South (78)", inMin: 138, walk: 8, isNext: false },
  { code: "MATH1051", time: "16:00", duration: "1hr", room: "67-141", building: "Steele (67)", inMin: 318, walk: 5, isNext: false },
];

const FlowTimetable = ({ go, openClass, showHints, openedClass, closeClass }) => (
  <div className="phone">
    <div className="phone-inner" style={{ position: "relative" }}>
      <div className="statusbar"><span>10:42</span><span>••• 􀟝􀛨</span></div>
      <div className="bb-chrome">
        <span className="bb-tag">≡ Blackboard</span>
        <span style={{ flex: 1 }}>Class Schedule</span>
        <span>⌕</span>
      </div>
      <div style={{ padding: "10px 14px 14px", display: "flex", flexDirection: "column", gap: 10, flex: 1, overflow: "hidden" }}>
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline" }}>
          <div style={{ fontSize: 26, fontWeight: 600 }}>Monday, 12 May</div>
          <div className="label">3 classes</div>
        </div>
        <div style={{ display: "flex", gap: 6 }}>
          {["M","T","W","T","F"].map((d, i) => (
            <div key={i} className={"box " + (i === 0 ? "fill-accent" : "soft")}
              style={{ flex: 1, textAlign: "center", padding: "6px 0", fontSize: 12 }}>
              {d}<br />
              <span style={{ fontSize: 10, color: "var(--pencil)" }}>{12 + i}</span>
            </div>
          ))}
        </div>

        {CLASSES.map((c, i) => (
          <Hotspot key={c.code} label={`tt:open-${c.code}`} onTap={() => openClass(c)} highlight={showHints && c.isNext && !openedClass} hint={c.isNext && !openedClass ? "tap your next class" : ""}>
            <div className={"box " + (c.isNext ? "fill-accent" : "")} style={{ padding: 10, display: "flex", gap: 10, alignItems: "center" }}>
              <div style={{ width: 50 }}>
                <div style={{ fontWeight: 700 }}>{c.time}</div>
                <div className="label" style={{ fontSize: 9 }}>{c.duration}</div>
              </div>
              <div style={{ flex: 1 }}>
                <div style={{ fontWeight: 600 }}>{c.code}</div>
                <div style={{ fontSize: 11, color: "var(--pencil)" }}>📍 {c.room}</div>
              </div>
              <div className="pill" style={{ fontSize: 10 }}>
                {c.isNext ? `in ${c.inMin}m` : `${c.walk}m walk`}
              </div>
            </div>
          </Hotspot>
        ))}

        <div style={{ marginTop: "auto", display: "flex", gap: 8, fontSize: 10, color: "var(--pencil)" }}>
          <Hotspot label="tt:demo-notif" onTap={() => go("notification")}>
            <div className="pill" style={{ fontSize: 10, pointerEvents: "none" }}>📱 see notification</div>
          </Hotspot>
        </div>
      </div>

      {/* class detail modal — rendered inside phone-inner so it's clipped properly */}
      {openedClass && (
        <ClassDetailModal klass={openedClass} go={go} onClose={closeClass} showHints={showHints} />
      )}
    </div>
  </div>
);

// Class detail modal — overlays the timetable phone (renders into the same .phone)
const ClassDetailModal = ({ klass, go, onClose, showHints }) => {
  if (!klass) return null;
  return (
    <>
      {/* dimmer */}
      <div
        onClick={onClose}
        style={{
          position: "absolute", inset: 0,
          background: "rgba(0,0,0,0.35)",
          zIndex: 5,
          animation: "fadeIn 0.22s ease-out",
        }}
      />
      {/* sheet */}
      <div style={{
        position: "absolute", left: 0, right: 0, bottom: 0,
        background: "var(--paper)",
        borderTop: "2px solid var(--ink)",
        borderRadius: "16px 16px 0 0",
        padding: 16,
        display: "flex", flexDirection: "column", gap: 10,
        animation: "modalSlideUp 0.32s cubic-bezier(0.2, 0.8, 0.2, 1)",
        zIndex: 10,
        boxShadow: "0 -8px 24px rgba(0,0,0,0.15)",
      }}>
        <div style={{ width: 40, height: 4, borderRadius: 2, background: "var(--pencil-light)", margin: "0 auto 4px" }} />
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
          <div>
            <div className="label">in {klass.inMin} min</div>
            <div style={{ fontSize: 22, fontWeight: 700, marginTop: 2 }}>{klass.code} · {klass.time}</div>
          </div>
          <Hotspot label="modal:close" onTap={onClose}>
            <div style={{ width: 32, height: 32, borderRadius: 16, border: "1.5px solid var(--pencil)", display: "grid", placeItems: "center", fontSize: 16, pointerEvents: "none" }}>×</div>
          </Hotspot>
        </div>

        <div className="box" style={{ display: "flex", gap: 10, alignItems: "center" }}>
          <div className="map-bg" style={{ width: 50, height: 50, borderRadius: 8, border: "1.5px dashed var(--pencil)" }} />
          <div style={{ flex: 1 }}>
            <div style={{ fontWeight: 600 }}>{klass.room}</div>
            <div style={{ fontSize: 11, color: "var(--pencil)" }}>{klass.building}</div>
          </div>
          <div className="pill accent" style={{ fontSize: 11 }}>{klass.walk}m walk</div>
        </div>

        <div className="box dashed soft" style={{ padding: 10 }}>
          <div className="label">recommended action</div>
          <div style={{ fontSize: 13, marginTop: 4 }}>
            Leave by <strong>{klass.isNext ? "10:54" : "12:52"}</strong> to arrive on time
          </div>
        </div>

        <Hotspot label={`modal:plan-${klass.code}`} onTap={() => go("slider")} highlight={showHints} hint="plan your departure">
          <button className="btn primary" style={{ width: "100%", pointerEvents: "none" }}>
            Plan my departure →
          </button>
        </Hotspot>
        <Hotspot label={`modal:walk-${klass.code}`} onTap={() => go("map")}>
          <button className="btn ghost" style={{ width: "100%", pointerEvents: "none" }}>
            I'm leaving now — start walking
          </button>
        </Hotspot>
      </div>
    </>
  );
};

window.FlowOnboardA = FlowOnboardA;
window.FlowOnboardB = FlowOnboardB;
window.FlowOnboardC = FlowOnboardC;
window.FlowTimetable = FlowTimetable;
window.ClassDetailModal = ClassDetailModal;
window.CLASSES = CLASSES;

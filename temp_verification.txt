const FlowMapScreen = ({ go, showHints, variant, onVariantChange }) => {
  const [progress, setProgress] = useWalkProgress(true);
  const [bldg, setBldg] = useState3("0062");
  const [floor, setFloor] = useState3("1");
  const eta = "10:58";
  useEffect3(() => { if (progress >= 1) setTimeout(() => go("indoor"), 600); }, [progress]);
  return (
    <div className="phone">
      <div className="phone-inner">
        <div className="statusbar"><span>10:54</span><span>••• 􀟝􀛨</span></div>
        <div className="bb-chrome">
          <span className="bb-tag">≡ Blackboard</span>
          <span style={{ flex: 1 }}>→ STAT2003 · 50-N202</span>
          <Hotspot label="map:cancel" onTap={() => go("timetable")}>
            <span style={{ pointerEvents: "none" }}>×</span>
          </Hotspot>
        </div>

        {/* compact route strip */}
        <div style={{ padding: "6px 12px 4px", display: "flex", alignItems: "center", gap: 8 }}>
          <div style={{ flex: 1 }}>
            <div className="label">walking · on track</div>
            <div style={{ fontSize: 18, fontWeight: 700 }}>arrive {eta}</div>
          </div>
          <div className="pill ok" style={{ fontSize: 11 }}>on time</div>
        </div>
        <div style={{ height: 3, margin: "0 12px 4px", background: "var(--pencil-light)", borderRadius: 2 }}>
          <div style={{ height: "100%", width: (progress * 100) + "%", background: "var(--accent)", borderRadius: 2, transition: "width 0.1s" }} />
        </div>

        {/* floor plan directly on the home screen */}
        <div style={{ padding: "2px 12px 3px", display: "flex", alignItems: "center", gap: 8 }}>
          <div style={{ height: 1, flex: 1, background: "var(--pencil-light)" }} />
          <div className="label" style={{ fontSize: 9, whiteSpace: "nowrap" }}>building floor plan</div>
          <div style={{ height: 1, flex: 1, background: "var(--pencil-light)" }} />
        </div>
        <IndoorPlan bldg={bldg} setBldg={setBldg} floor={floor} setFloor={setFloor} />

        <div style={{ padding: "8px 12px" }}>
          <Hotspot label="map:arrived" onTap={() => go("arrived")} highlight={showHints} hint="tap when at the room">
            <button className="btn primary" style={{ width: "100%", pointerEvents: "none" }}>I made it ✓</button>
          </Hotspot>
        </div>
      </div>
    </div>
  );
};

